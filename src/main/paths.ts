import { join } from 'path';

export const ROOT_DIR = join(__dirname, '..');
export const RENDERER_DIR = join(ROOT_DIR, 'renderer');
export const PRELOAD_DIR = join(ROOT_DIR, 'preload');
export const MAIN_DIR = join(ROOT_DIR, 'main');